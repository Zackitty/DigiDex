import { Injectable } from '@angular/core';

@Injectable({
  providedIn: 'root'
})
export class FilterService {

  constructor() { }
}

// should have a filter for a name
//  should have a filter by stage
// should have a filter by type
// should have a filter by attribute
// should have a fitler that picks a greater than or less than a number for the
// rest of the stats


